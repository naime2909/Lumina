{
  "version": 2,
  "buildCommand": "cd sonic-lumina-controller && npm install && npm run build",
  "outputDirectory": "sonic-lumina-controller/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
